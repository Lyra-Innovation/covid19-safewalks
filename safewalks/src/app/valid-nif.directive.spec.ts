import { ValidNIFDirective } from './valid-nif.directive';

describe('ValidNIFDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidNIFDirective();
    expect(directive).toBeTruthy();
  });
});
